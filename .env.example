# ===========================================
# TrustTax VA – Variables de entorno
# ===========================================
# Copia este archivo a .env y pega tus valores reales.
# Nunca subas .env a Git.

# ----- Base de datos (Prisma) -----
# Ejemplo local: postgresql://usuario:password@localhost:5432/trusttax?schema=public
# Ejemplo Prisma/hosted: postgres://HASH:sk_XXX@db.prisma.io:5432/postgres?sslmode=require
DATABASE_URL="postgres://TU_HASH:TU_KEY@db.prisma.io:5432/postgres?sslmode=require"

# ----- API (NestJS) -----
# Opcionales; por defecto PORT=4000, JWT_SECRET=secretKey (solo dev)
# PORT=4000
# JWT_SECRET=tu-secret-min-32-caracteres

# ----- Seed admin principal (solo para seed-main-admin) -----
# SEED_MAIN_ADMIN_EMAIL=admin@ejemplo.com
# SEED_MAIN_ADMIN_PASSWORD=tu-password-seguro
# SEED_MAIN_ADMIN_NAME=Admin Principal

# ----- Frontend (Vite – web-client, web-admin) -----
# VITE_API_URL=http://localhost:4000
